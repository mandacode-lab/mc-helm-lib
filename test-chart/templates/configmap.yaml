{{- define "test-chart.config" -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "test-chart.fullname" . }}-api-config
  labels:
    {{- include "mc-helm-lib.labels" (dict "component" "api" "root" .) | nindent 4 }}
data:
  HTTP_PORT: "8080"
  HTTP_HOST: "0.0.0.0"
  LOG_LEVEL: "info"
{{- end -}}

{{ include "test-chart.config" . }}
